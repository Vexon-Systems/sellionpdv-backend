# Ativado explicitamente em desenvolvimento local (SPRING_PROFILES_ACTIVE=dev),
# também usado pelo DataSeeder (@Profile({"dev","test","local"})).

# SAST-16: verboso de propósito aqui — só em dev, nunca por padrão em prod/staging.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
